apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rapp.fullname" . }}-config
  labels:
    {{- include "rapp.labels" . | nindent 4 }}
data:
  rapp.yaml: |
{{ tpl .Values.configYaml . | nindent 4 }}
