# Built packages carry the chart, and the chart carries real values and
# secrets. Treat everything here as sensitive: never commit it, never publish
# it, never attach it to an issue.
dist/
*.csar
*.tgz

/[[.Name]]
